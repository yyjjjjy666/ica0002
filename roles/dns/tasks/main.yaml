---
  - name: disable systemd-resolved
    service:
      name: systemd-resolved
      state: stopped
      enabled: no

  - name: resolv.conf
    template:
      src: resolv.conf
      dest: /etc/resolv.conf
