- name: Install uWSGI packages
  apt: 
    name: 
      - uwsgi-plugin-python3
      - uwsgi
    state: present

- name: Start service
  service:
    name: uwsgi
    state: started
    enabled: yes

- name: Add the configuration for agama
  copy:
    src: /home/administrator/ica0002/roles/uwsgi/files/agama.ini
    dest: /etc/uwsgi/apps-enabled/agama.ini
  notify: Restart uwsgi